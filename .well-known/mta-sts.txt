version: STSv1
mode: enforce # 1. Decide your mode. [enforce|testing|none]
mx: mx1.forwardemail.net      # 2. Add your primary mailserver hostname (see MX records in your DNS)
mx: mx2.forwardemail.net      # 3. If you have secondary mailservers, add one line for each.
max_age: 604800              # 4. Depending on your mode above, 604800 seconds = 1 week might be a good value.
